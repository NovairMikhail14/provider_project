import 'package:flutter/material.dart';

class Movie{
  String title;
  String runtime;
  bool  like;
  Color color;
  Movie({required this.title,required this.runtime,required this.like,required this.color});
}